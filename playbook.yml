---
  - hosts: fullapache2
    sudo: true
    roles:
      - base
      - cron-apt
      - ntp
      - git
      - ipv4harden
      - ipv6disable
      - apache
      - php-fpm
      - php
      - apache-fastcgi
      - mysql
      - modsecurity
      - fail2ban
      - bacula-fd
      
  - hosts: base
    sudo: true
    roles:
      - base
      - cron-apt
      - ntp
      - git
      - ipv4harden
      - ipv6disable
